% 论文译文 LaTeX 模板示例
% 复制到工作区根目录（例如 latex-template.tex），并在 paper-reading.config.yml 中设置：
%   latex_template: latex-template.tex
%
% 必填占位符：正文 body（双花括号）；可选 title / authors / glossary / notes / references。
% 注意：注释中不要写字面双花括号占位符，否则 assemble 全局替换时会破坏注释。
% 编译时工作目录为 <slug>/translation/，插图路径使用 figures/... 等相对路径。

\documentclass[12pt]{ctexart}
\usepackage[margin=2.5cm]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs,array}
\usepackage[hidelinks]{hyperref}
\usepackage{xcolor}
\setlength{\parskip}{0.6em}

\begin{document}
\title{{{title}}}
\author{{{authors}}}
\date{}
\maketitle

{{body}}

% 若保留下列附录占位符，由模板单独排版；若删除，脚本会把对应内容并入正文末尾。
{{glossary}}
{{notes}}
{{references}}

\end{document}
